Highlighting (this run has it enabled):
- In each major section, wrap the single sentence a reader should remember
  most — the one key takeaway if they read nothing else in that section —
  in ==...==, including that sentence's own trailing citation marker(s):
  "==The authorization code expires within ten minutes of issuance.[3]==".
- At most one highlighted sentence per section, and it must be a single
  sentence, never a whole paragraph or the executive summary. Skip a section
  entirely if nothing in it stands out more than the rest — a report where
  everything is highlighted has highlighted nothing.
- Never highlight a heading or a list item, only a sentence of body prose.
