Highlighting (this run has it enabled):
- In each major section, wrap the 2-3 consecutive sentences a reader should
  remember most — the key takeaway if they read nothing else in that
  section, with enough of its own surrounding context to stand alone — in
  ==...==, including any citation marker(s) inside that span:
  "==The authorization code expires within ten minutes of issuance.[3] This
  is shorter than the refresh token's 30-day window, so clients must treat
  the two as separate rotation schedules.[4]==".
- At most one highlighted passage per section, and it must be 2-3
  consecutive sentences from the same paragraph — never a single sentence,
  never a whole paragraph, and never the executive summary. Skip a section
  entirely if nothing in it stands out more than the rest — a report where
  everything is highlighted has highlighted nothing.
- Never highlight a heading or a list item, only sentences of body prose.
